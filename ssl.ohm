SSL {
Main = Def+

Def = name "=" number ";"

name = letter alnum*
number =
  | "-" digit+ -- negative
  |     digit+ -- positive
  
comment =
  | "{" dontcareChar* "}" -- nested
  
dontcareChar = ~"}" ~"{" any
space += comment
}

